# 后端 API 地址（浏览器直连，后端 dev 已自动放行局域网 CORS）
# 通过 http://192.168.x.x:3000 访问时，可留空或保持 localhost，前端会自动改用同 IP:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Web 默认提供页内窗口缩放；桌面客户端构建时设为 false。
NEXT_PUBLIC_ENABLE_WINDOW_SCALING=true
